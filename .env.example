# Copiar a .env y ajustar. NUNCA subir .env al repositorio.
DATABASE_URL=postgresql+psycopg://platano:platano@db:5432/platano
APP_ENV=dev
LOG_LEVEL=INFO
# Ruta del archivo de parámetros del dominio (tiempos de acarreo, capacidades, coeficientes)
PARAMETROS_PATH=config/parametros.yaml
