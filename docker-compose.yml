services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: platano
      POSTGRES_PASSWORD: platano
      POSTGRES_DB: platano
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U platano"]
      interval: 5s
      retries: 5

  app:
    build: .
    depends_on:
      db:
        condition: service_healthy
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - .:/code

volumes:
  pgdata:
